import software.bernie.geckolib.renderer.GeoEntityRenderer;

public class Test {
    // This will show the required signature in error
    public static class TestRenderer extends GeoEntityRenderer {
    }
}
